# X記事 投稿用原稿: Claude Code 完全入門ガイド

## 投稿手順

1. X（PC版）のサイドバーから「記事」を選択
2. 下記の【タイトル】をタイトル欄に入力
3. 下記の【本文】を本文欄にコピペ
4. 見出しを「大見出し」に設定
5. 公開ボタンを押す
6. 公開と同時に【告知ポスト】をポストする

---

## 【タイトル】

【保存版】Claude Code 完全入門ガイド — インストールから「AIに開発を任せる」までの全手順

---

## 【本文】


はじめに

「Claude Code、名前は聞くけど何ができるの？」
「興味あるけど、始め方が分からない」
「エンジニアじゃないと使えないんでしょ？」

全部答える。

この記事では、Claude Codeのインストールから初めてのプロジェクト完成まで、画面の前で迷わないレベルで全手順を書いた。

この記事を保存して、上から順にやるだけでいい。


Claude Codeとは？（30秒で分かる説明）

普通のAIチャット（ChatGPTやClaude）:
→ 質問すると回答が返ってくる。コピペして自分で作業する。

Claude Code:
→ 指示を出すとAIが自分でファイルを開き、コードを書き、テストを実行し、Gitコミットまで完了する。

つまり「AIに仕事を丸ごと任せられるツール」。

NASAの火星探査車のルート計画にも使われている。Anthropic社内では弁護士がツールを構築し、マーケターが広告を自動生成している。エンジニア専用のツールではない。


必要なもの

始める前に確認すること:

① パソコン（Mac / Windows / Linux どれでもOK）
② Claude のアカウント（Pro以上のプラン。月$20〜）
③ Node.js（バージョン18以上）
④ ターミナルを開ける程度の基本操作

「ターミナルって何？」という人でも大丈夫。Macなら「ターミナル」アプリ、Windowsなら「PowerShell」を開くだけ。


Step 1: インストール（3分）

ターミナルを開いて、以下のコマンドを入力する:

npm install -g @anthropic-ai/claude-code

これだけ。インストールが完了する。

「npm が見つかりません」とエラーが出た場合:
→ Node.jsがインストールされていない。nodejs.org からダウンロードして入れてから再実行。


Step 2: 初回起動と認証（2分）

ターミナルで以下を入力:

claude

初めて起動すると認証画面が出る。Claudeアカウントでログインする。

ブラウザが自動で開くので、画面の指示に従って認証を完了させる。「Approved」と表示されたらOK。


Step 3: プロジェクトフォルダで起動する

Claude Codeは「今いるフォルダ」を認識する。

既存のプロジェクトがある場合:
cd プロジェクトのフォルダパス
claude

新しくプロジェクトを始める場合:
mkdir my-project
cd my-project
claude

ここがポイント。Claude Codeは起動したフォルダの中身を全部読める。だから「このプロジェクト全体を理解した上で」作業してくれる。


Step 4: 最初の指示を出してみる

起動したら、日本語で指示を出すだけ。

例1（初心者向け）:
「簡単なToDoアプリをReactで作って。追加、削除、完了マークの機能をつけて」

例2（既存プロジェクトがある場合）:
「このプロジェクトの構成を説明して」

例3（バグ修正）:
「npm run dev で起動するとエラーが出る。原因を調べて修正して」

Claude Codeはこの指示を受けて:
→ 必要なファイルを作成する
→ コードを書く
→ 依存パッケージをインストールする
→ テストを実行する
→ エラーがあれば自分で修正する

全部自動。


Step 5: CLAUDE.md を作る（これが超重要）

Claude Codeを「ただのAI」から「自分専用の開発パートナー」に変える最大のコツがこれ。

プロジェクトのルートに CLAUDE.md というファイルを作る。

CLAUDE.md に書くこと:
・プロジェクトの概要（何を作っているか）
・使っている技術スタック
・コーディング規約（命名規則、インデント等）
・テストの実行方法
・よく使うコマンド
・注意事項（触ってはいけないファイル等）

例:

# プロジェクト概要
ECサイトのバックエンドAPI

# 技術スタック
- TypeScript
- Express.js
- PostgreSQL
- Prisma ORM

# コーディング規約
- 変数名はcamelCase
- 関数はアロー関数で統一
- エラーハンドリングは必ずtry-catchで包む

# テスト
npm run test で全テスト実行

# 注意事項
- .env ファイルは絶対にコミットしない
- migrationは手動で確認してから実行

Claude Codeは起動時にこのファイルを自動で読み込む。これがあるだけで、指示の精度が劇的に上がる。


Step 6: 便利なスラッシュコマンド

Claude Codeにはショートカットコマンドがある。覚えておくと効率が上がる:

/init — プロジェクトの初期分析。CLAUDE.mdの自動生成
/compact — 会話の要約。コンテキストが長くなった時に使う
/clear — 会話をリセット
/voice — 音声入力モード（最新機能。声でコーディングできる）
/cost — 現在のセッションで使ったトークン量を確認
/help — コマンド一覧を表示

特に /init は最初に必ず実行すること。プロジェクトを自動解析してCLAUDE.mdの下書きを作ってくれる。


Step 7: MCP連携で「手足」を生やす

MCP（Model Context Protocol）を設定すると、Claude Codeが外部ツールと連携できるようになる。

設定方法:
プロジェクトルートに .mcp.json を作成して、使いたいサーバーを記述する。

よく使われるMCP連携:
・Playwright — ブラウザを自動操作。Webアプリのテストに
・GitHub — Issue、PRの操作を自動化
・Notion — ドキュメントの読み書き
・PostgreSQL — データベースへの直接クエリ
・Slack — メッセージの送受信

MCPを入れると、Claude Codeが「コードを書くだけ」から「ブラウザも動かせる、DBも触れる、Slackにも投稿できる」ツールに進化する。


Step 8: カスタムスキルを作る

自分だけのコマンドを作れる。

.claude/commands/ フォルダにMarkdownファイルを置くだけ。

例: .claude/commands/review.md

---
description: コードレビューを実行する
---
変更されたファイルを全て確認し、以下の観点でレビューしてください:
1. セキュリティ上の問題
2. パフォーマンスの懸念
3. コーディング規約との整合性
4. テストの網羅性
問題があれば修正案も提示してください。

これで /review と打つだけでカスタムレビューが実行される。

チームで共有すれば、レビュー基準が統一される。


実践: よく使う指示テンプレート10選

そのままコピペして使えるテンプレート:

① プロジェクト理解
「このプロジェクトの全体構成を説明して。主要なファイルとその役割を一覧にして」

② バグ修正
「npm run dev で以下のエラーが出る。原因を特定して修正して: [エラーメッセージを貼り付け]」

③ 新機能追加
「ユーザー認証機能を追加して。JWT認証で、ログイン・ログアウト・トークンリフレッシュを実装して」

④ テスト作成
「src/api 配下の全エンドポイントに対するユニットテストを作成して。カバレッジ80%以上を目指して」

⑤ リファクタリング
「このファイルが500行を超えている。責務ごとにファイルを分割して」

⑥ ドキュメント生成
「APIのエンドポイントを全て洗い出して、OpenAPI仕様書を生成して」

⑦ セキュリティチェック
「このプロジェクト全体をスキャンして、セキュリティ上の問題を報告して。SQLインジェクション、XSS、CSRF等を確認して」

⑧ パフォーマンス改善
「このAPIのレスポンスが遅い。ボトルネックを特定して改善案を実装して」

⑨ データベース操作
「usersテーブルに roles カラムを追加するマイグレーションを作成して」

⑩ デプロイ準備
「本番デプロイ前のチェックリストを作成して。環境変数、ビルド、テスト、セキュリティの観点で」


料金の目安

Claude Codeを使うには Claude のサブスクリプションが必要:

Pro（$20/月）: 使えるが制限が厳しい。軽い作業向け
Max 5x（$100/月）: 本気で開発するならこれが最低ライン
Max 20x（$200/月）: ヘビーユーザー向け。1日中使っても余裕
Team（$30/ユーザー/月）: チーム利用向け
Enterprise: 大規模組織向け。要問い合わせ

個人的な感覚だと:
・週に数回使う程度 → Pro で十分
・毎日の開発に組み込む → Max 5x 推奨
・Claude Codeメインで開発する → Max 20x が快適

$100/月は高く感じるかもしれないが、時給2,000円のエンジニアが1日2時間節約できれば月80,000円分の価値。ROIは約6倍。


やってはいけないこと

Claude Codeを使う上での注意点:

① 機密情報を含むファイルをそのまま読ませない
→ .env ファイル等はCLAUDE.mdで「触らないで」と明記する

② AIの出力を無検証でデプロイしない
→ 必ず人間がレビューする。特にセキュリティ関連

③ 全部丸投げにしない
→ 設計の方針は人間が決める。AIは「実装」担当

④ コンテキストを溜めすぎない
→ 長時間使っていると精度が落ちる。/compact で定期的に圧縮する

⑤ 最新のライブラリ情報を過信しない
→ 学習データにタイムラグがある。公式ドキュメントで裏取りする


まとめ: 今日やること

この記事を読んで「やってみよう」と思った人は、今日これだけやってほしい:

1. npm install -g @anthropic-ai/claude-code でインストール
2. プロジェクトフォルダで claude を起動
3. /init でプロジェクトを自動解析
4. 「このプロジェクトの構成を説明して」と聞いてみる

ここまで10分。

一度体験したら、もう戻れなくなる。

この記事を保存して、困った時に読み返してください。質問があればリプライで聞いてください。

---

## 【告知ポスト】

```
Claude Code、名前は知ってるけど
「始め方が分からない」って人が多すぎる。

なので完全入門ガイドを記事にまとめた。

・インストール（3分）
・初回起動と認証（2分）
・CLAUDE.md の書き方（これで精度が激変する）
・MCP連携で外部ツールと接続
・カスタムスキルの作り方
・そのまま使える指示テンプレート10選
・料金の目安とROI計算

上から順にやるだけ。

保存して今日から始めてみて↓
```

---

## 【翌日のフォローアップ投稿】

### パターンA: 反響が大きかった場合
```
昨日のClaude Code入門記事、
過去最高の保存数をもらった。ありがとう。

特に反応が多かったのが
「CLAUDE.md」の話。

Claude Codeを使ってて
「なんか精度イマイチだな」って人、
CLAUDE.md 作ってないのが原因かも。

プロジェクトの概要と規約を書いたファイルを
ルートに置くだけで、指示の理解度が別次元になる。

まだの人は今日5分で作ってみて。
```

### パターンB: 質問が多かった場合
```
Claude Code入門記事への質問で多かったもの:

Q. Proプラン($20)で実用的？
A. 週2-3回の軽い使い方なら十分。毎日ガッツリなら Max($100)推奨

Q. エンジニアじゃなくても使える？
A. 使える。Anthropic社内で弁護士やマーケターも使ってる

Q. CursorやCopilotとの違いは？
A. Cursor = エディタ内で補完。Claude Code = ターミナルで自律的に開発を完走

Q. Windows でも動く？
A. 動く。Mac/Windows/Linux全対応

他にも質問あったらどうぞ。
```

### パターンC: 実践の続編へ誘導
```
昨日Claude Code入門の記事を出したけど、
実は「本当にヤバい使い方」はまだ書いてない。

次の記事で書く予定のネタ:

・Claude Codeに声でコーディングさせる方法（Voice Mode）
・GitHub Actions連携で自動PR作成
・MCPでブラウザテストを完全自動化
・サブエージェントで専門家チームを構築

どれが気になる？
一番リプライ多かったやつから書きます。
```
